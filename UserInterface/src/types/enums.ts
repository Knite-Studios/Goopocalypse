export enum GameState {
    Menu,
    Lobby,
    Loading,
    Playing,
    Paused,
    GameOver
}

export enum PlayerRole {
    None,
    Buddie,
    Fwend
}

export enum MenuState {
    Local,
    Online
}

export enum DisplayMode {
    Fullscreen,
    Borderless,
    Windowed
}
